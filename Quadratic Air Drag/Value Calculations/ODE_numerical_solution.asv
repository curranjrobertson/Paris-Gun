% Attempt to solve 24 m/s < Vy < Mach 1 
% constants
c2 = 0.008; % drag coefficient (change)
g = 9.81; % acceleration due to gravity in m/s^2



tspan = [0 5]; % timespan in seconds (change)
y0 = 100; % initial y-value (change)

dydt1 = y1
dydt2 = @(t,y) c2*y1^2 + g;

syms y(t)
[V] = odeToVectorField(



% syms y(t)
% ypp = diff(y, 2) == c2*y^2 + g;



% ypp = @(t,y) c2*y^2 + g;
% 
% Solve 
[t,y] = ode45(dydt2, tspan, y0)
% 
plot(t,y)

